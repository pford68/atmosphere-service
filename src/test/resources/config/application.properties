# Active profile
spring.profiles.active=test

# Logging
logging.level.com.masterpeace=INFO
logging.level.org.springframework.security=DEBUG

# Datasource configuration
spring.datasource.url=jdbc:mysql://localhost/atmosphere_test
spring.datasource.username=atmuser
spring.datasource.password=allyewhoenterhereabandonallhope
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# Hibernate settings
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.naming-strategy=org.hibernate.cfg.ImprovedNamingStrategy
#spring.jpa.show-sql=true

# Endpoints
endpoints.shutdown.enabled=false


# Atmosphere settings
atmosphere.cors.origin=*


# Messages
atmosphere.deletion.started=Another admin has requested to delete %s #%d.
atmosphere.deletion.completed=The %s #%d has been deleted.