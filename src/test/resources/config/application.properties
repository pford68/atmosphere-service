# Active profile
spring.profiles.active=test

# Logging
logging.level.com.masterpeace=INFO
logging.level.org.springframework.security=DEBUG

# Datasource configuration
spring.datasource.url=jdbc:mysql://localhost/atmosphere_test?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username=atm
spring.datasource.password=macedon
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.initialization-mode=always

# Hibernate settings
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false


# Atmosphere settings
atmosphere.cors.origin=*


# Messages
atmosphere.deletion.started=Another admin has requested to delete %s #%d.
atmosphere.deletion.completed=The %s #%d has been deleted.